Git clone "https://github.com/ASParvathy/sample.git"

Git and Github

alternatives: AWS ,bitbucket,gitlab

git.add  >>> add the file to the starting alternatives(file being tracked by git)

git commit-m "message"   >> to commit the changes in the tracked file

git push origin main >> pushing the file to the main branch in github respository

git add filename >> specific file

git add .
 